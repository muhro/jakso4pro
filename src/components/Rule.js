import React from 'react'



    const Rule = ({ visible }) => (

            <div id="navbar"
            className={visible ? 'slideIn' : 'slideOut'}>
            Hello
        </div>

    )



export default Rule;